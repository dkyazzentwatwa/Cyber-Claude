# Cyber-Claude Configuration

# AI Provider API Keys (at least one required)
ANTHROPIC_API_KEY=your_anthropic_api_key_here
GOOGLE_API_KEY=your_google_api_key_here

# Default Model (optional)
# Options: claude-sonnet-4-5, claude-opus-4-1, gemini-2.5-flash, etc.
MODEL=claude-sonnet-4-5

# ====================================
# MCP Security Tool Configuration
# ====================================
# Enable MCP servers by setting to 'true'
# MCP servers are auto-installed via npx when first used

# Web Security Tools
MCP_NUCLEI_ENABLED=false           # Vulnerability scanning (5000+ templates)
MCP_SSLSCAN_ENABLED=false          # SSL/TLS security analysis
MCP_HTTP_HEADERS_ENABLED=false     # HTTP security headers analysis
MCP_SQLMAP_ENABLED=false           # SQL injection testing
MCP_HTTPX_ENABLED=false            # HTTP probing and tech detection
MCP_KATANA_ENABLED=false           # Web crawler with JS parsing
MCP_FFUF_ENABLED=false             # Fast web fuzzer for content discovery
MCP_WPSCAN_ENABLED=false           # WordPress vulnerability scanner

# Mobile Security Tools
MCP_MOBSF_ENABLED=false            # Mobile Security Framework (Android/iOS analysis)

# Network Security Tools
MCP_NMAP_ENABLED=false             # Network scanning and service detection
MCP_MASSCAN_ENABLED=false          # Ultra-fast port scanning

# Reconnaissance Tools
MCP_AMASS_ENABLED=false            # Subdomain enumeration
MCP_GOWITNESS_ENABLED=false        # Web screenshot and visual reconnaissance
MCP_CERO_ENABLED=false             # Certificate transparency domain enumeration

# ====================================
# Usage Examples
# ====================================
#
# Enable Nuclei for vulnerability scanning:
#   MCP_NUCLEI_ENABLED=true npm start -- webscan https://example.com --nuclei
#
# Enable SSL/TLS analysis:
#   MCP_SSLSCAN_ENABLED=true npm start -- webscan https://example.com --sslscan
#
# Enable network scanning with Nmap:
#   MCP_NMAP_ENABLED=true npm start -- scan --network --nmap
#
# Enable SQL injection testing:
#   MCP_SQLMAP_ENABLED=true npm start -- webscan https://example.com --sqlmap
#
# Enable web fuzzing:
#   MCP_FFUF_ENABLED=true npm start -- (requires webscan integration)
#
# Enable WordPress scanning:
#   MCP_WPSCAN_ENABLED=true npm start -- (requires webscan integration)
#
# Enable mobile app security scanning:
#   MCP_MOBSF_ENABLED=true npm start -- mobilescan app.apk
#
# Enable visual reconnaissance:
#   MCP_GOWITNESS_ENABLED=true npm start -- (requires recon integration)
#
# Enable certificate transparency search:
#   MCP_CERO_ENABLED=true npm start -- (requires recon integration)
#
# Enable all web security tools:
#   MCP_NUCLEI_ENABLED=true \\
#   MCP_SSLSCAN_ENABLED=true \\
#   MCP_SQLMAP_ENABLED=true \\
#   MCP_FFUF_ENABLED=true \\
#   MCP_WPSCAN_ENABLED=true \\
#   npm start -- webscan https://example.com --full